const { Router } = require("express");
const axios = require("axios")
const {Pokemon, Types} = require("../db")

const {
    getAllPokemons,
    getAllPokemonsId,
    getAllPokemonsName
} = require()
